{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ../static/css/bundle.css --minify",
    "build:js": "esbuild index.js --bundle --minify --outfile=../static/js/bundle.js",
    "build": "npm run build:css && npm run build:js",
    "watch": "concurrently \"npm run build:css -- --watch\" \"npm run build:js -- --watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^7.0.0",
    "alpinejs": "^3.14.9",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.8",
    "esbuild-plugin-postcss2": "^0.1.2",
    "htmx.org": "^2.0.6",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "postcss-url": "^10.1.3",
    "tailwindcss": "^3.4.17"
  }
}
